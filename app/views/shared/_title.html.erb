  
<div class="flex items-center justify-between mb-12">
  <h3 class="text-2xl font-bold leading-none tracking-tight"><%= title %></h3>
  <% if (defined?(path) && path) && (current_user.admin? || current_user.sale?) %>
    <%= render_button "Adicionar", as: :link, href: path, class: "md:px-12" %>
  <% end %>
</div>